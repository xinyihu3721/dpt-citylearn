# Verified working combination on Python 3.10 (see CLAUDE.md "Gate 0.5" / "Gate 1" for how this
# pin was arrived at -- citylearn 2.1b12 specifically, NOT the latest release, is required because
# oracle/chesca_repo targets citylearn's older pre-Gymnasium API).
citylearn==2.1b12
gym==0.26.2
gymnasium==1.3.0

torch==2.12.0          # install the CUDA build matching your own hardware, e.g. via
                        # https://pytorch.org/get-started/locally/ -- the exact cuXXX wheel tag
                        # is hardware/driver-specific and not reproduced here
numpy==1.26.4
pandas==2.0.3
scipy==1.15.2
xgboost==3.2.0
matplotlib==3.10.9
PyYAML==6.0.3
